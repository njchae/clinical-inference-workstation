"""Design-of-Experiments parameter-sweep tooling.

Formalizes the repository's existing per-pathway discipline — temperature
calibration and sensitivity-constrained threshold selection (see
``ml.pipeline``) — into a reusable grid sweep so configurations can be compared
systematically instead of one run at a time.
"""
